import styled from 'styled-components';

export default styled('div')`
  flex-basis: 100%;
  height: 35rem;
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  height: auto;
  transform: translateY(-1rem);
`;
