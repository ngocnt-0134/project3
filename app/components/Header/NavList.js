import styled from 'styled-components';
import Ul from '../List/Ul';

export default styled(Ul)`
  align-items: center;
  font-size: 1.2rem;
  display: flex;
  padding: 0;
  position: relative;
  z-index: 3;
`;
